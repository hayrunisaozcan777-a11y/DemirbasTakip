@model IEnumerable<DemirbasTakip.Models.Zimmet>
@{
    ViewData["Title"] = "Raporlar";
}

<h3 class="mb-4">Zimmet Raporu</h3>

<form method="get" class="row g-2 mb-4">
    <div class="col-md-3">
        <label class="form-label">Personel</label>
        <select name="personelId" class="form-select" asp-items="ViewBag.Personeller">
            <option value="">Tüm Personel</option>
        </select>
    </div>
    <div class="col-md-3">
        <label class="form-label">Demirbaş</label>
        <select name="demirbasId" class="form-select" asp-items="ViewBag.Demirbaslar">
            <option value="">Tüm Demirbaşlar</option>
        </select>
    </div>
    <div class="col-md-2">
        <label class="form-label">Başlangıç Tarihi</label>
        <input type="date" name="baslangic" value="@ViewBag.Baslangic" class="form-control" />
    </div>
    <div class="col-md-2">
        <label class="form-label">Bitiş Tarihi</label>
        <input type="date" name="bitis" value="@ViewBag.Bitis" class="form-control" />
    </div>
    <div class="col-md-2 d-flex align-items-end">
        <button type="submit" class="btn btn-outline-secondary me-2">Filtrele</button>
        <a asp-action="Index" class="btn btn-outline-dark">Temizle</a>
    </div>
</form>

<div class="row g-3 mb-4">
    <div class="col-md-4">
        <div class="dashboard-card" style="background-color:#6c757d;">
            <div class="small">Toplam Kayıt</div>
            <h3>@ViewBag.ToplamKayit</h3>
        </div>
    </div>
    <div class="col-md-4">
        <div class="dashboard-card" style="background-color:#198754;">
            <div class="small">Zimmetli</div>
            <h3>@ViewBag.AktifSayisi</h3>
        </div>
    </div>
    <div class="col-md-4">
        <div class="dashboard-card" style="background-color:#dc3545;">
            <div class="small">İade Edildi</div>
            <h3>@ViewBag.IadeSayisi</h3>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Personel</th>
                    <th>Demirbaş</th>
                    <th>Zimmet Tarihi</th>
                    <th>İade Tarihi</th>
                    <th>Durum</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var z in Model)
                {
                    <tr>
                        <td>@z.Personel?.AdSoyad</td>
                        <td>@z.Demirbas?.Ad</td>
                        <td>@z.ZimmetTarihi.ToString("dd.MM.yyyy HH:mm")</td>
                        <td>@(z.IadeTarihi?.ToString("dd.MM.yyyy HH:mm") ?? "-")</td>
                        <td>
                            @if (z.Durum == DemirbasTakip.Models.ZimmetDurumu.Aktif)
                            {
                                <span class="badge bg-success">Zimmetli</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">İade Edildi</span>
                            }
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr><td colspan="5" class="text-center text-muted py-4">Kayıt bulunamadı.</td></tr>
                }
            </tbody>
        </table>
    </div>
</div>